<li>
  <%= image_fu(item.image, '637x421#', {:id => dom_id(item), :title => item.title}) %>
  <% if ::Refinery::Portfolio.display_item_text %>
    <div class='label_text'>
      <h2><%= item.title %></h2>
    </div>
  <% end %>
</li>

